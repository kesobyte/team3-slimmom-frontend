import styled from 'styled-components';
// Added Dependencies styled-components & styled-system
import {
  color,
  layout,
  flexbox,
  typography,
  border,
  shadow,
  space,
  position,
  grid,
} from 'styled-system';

export const ContentDesignSup = styled.div`
  ${color};
  ${layout};
  ${flexbox};
  ${typography};
  ${border};
  ${shadow};
  ${space};
  ${position};
  ${grid};
`;
